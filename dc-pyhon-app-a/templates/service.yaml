apiVersion: v1
kind: Service
metadata:
  name: {{ include "dc-pyhon-app-a.fullname" . }}
  labels:
    {{- include "dc-pyhon-app-a.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "dc-pyhon-app-a.selectorLabels" . | nindent 4 }}
